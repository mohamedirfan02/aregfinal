specVersion: "v1beta"
serviceId: "aregapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "aregapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
